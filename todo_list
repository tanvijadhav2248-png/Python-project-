# todo_list.py
tasks = []

while True:
    task = input("Enter a task (or 'q' to quit): ")
    if task.lower() == 'q':
        break
    tasks.append(task)

print("\nYour To-Do List:")
for i, t in enumerate(tasks, 1):
    print(f"{i}. {t}")
